// components/gate/gateTimings.ts

/**
 * The one gate timing JS needs to know. Everything else lives in the gate block
 * in src/index.css — keep this in step with the 62% swap point of the 2.12s
 * `gate-site-reveal` / `gate-cover-out` pair.
 */
export const GATE_SWAP_MS = 1320;
