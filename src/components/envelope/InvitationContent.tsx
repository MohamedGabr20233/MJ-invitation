// components/envelope/InvitationContent.tsx

/**
 * The actual invitation, revealed once the envelope opens.
 * Replace the markup below with the real invitation page.
 */
const InvitationContent = () => {
  return (
    <div className="flex h-full flex-col items-center justify-center">
      <h1 className="text-4xl">Mohamed &amp; J</h1>

      <p className="mt-4">Wedding Invitation</p>
    </div>
  );
};

export default InvitationContent;
